<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- Hériter du wizard générique d'envoi de mails pour tous les modèles -->
        <record id="mail_compose_message_form_recipients_info" model="ir.ui.view">
            <field name="name">mail.compose.message.form.recipients.info</field>
            <field name="model">mail.compose.message</field>
            <field name="inherit_id" ref="mail.email_compose_message_wizard_form"/>
            <field name="arch" type="xml">
                <!-- Ajouter l'affichage des destinataires après le champ partner_ids -->
                <field name="partner_ids" position="after">
                    <div class="alert alert-info" role="alert" attrs="{'invisible': [('recipients_info', '=', False)]}" style="margin-top: 10px;">
                        <!-- <h6>👥 Destinataires du mail : </h6> -->
                        <field name="recipients_info" readonly="1" nolabel="1"/>
                     </div>
                </field>
                <!-- Modifier le texte pour les abonnés -->
                <span name="document_followers_text" position="replace">
                    <span name="document_followers_text" attrs="{'invisible':['|', ('model', '=', False), ('composition_mode', '=', 'mass_mail')]}">
                        <!-- <strong style="color: #0d47a1;">Abonnés du document et</strong> -->
                    </span>
                </span>
            </field>
        </record>
    </data>
</odoo>